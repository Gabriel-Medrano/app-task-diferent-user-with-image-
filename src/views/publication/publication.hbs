<div class="contanier my-4">
    <div class="row">
        <div class="card">
            <img src="{{publication.path}}" alt="" class="card-img-top" >
            <div class="card-body">
                <h5 class="card-title text-center border-bottom">{{publication.title}}</h5>
                <p class="card-text">{{publication.description}}</p>
                <p class="card-footer">{{publication.createdAt}}</p>
                <a href="/publication/remove/{{publication._id}}" class="btn btn-danger d-block">Delete Image</a>
            </div>
        </div>
    </div>
 </div>